<?php

// Please copy this file to conf.php and adjust settings

if (isset($config))
{
	$config->ftp_enabled = 1;
	$config->ftp_host = '127.0.0.1';
	$config->ftp_port = '21';
	$config->ftp_user = '';
	$config->ftp_pass = '';
	$config->ftp_root = ''; // Please test in a subfolder. No trailing slash
	//$config->ftp_syst = 'WIN'; // If not set, you will be asked to manually verify the output of the SYST command
}

// Please test both FTP_NATIVE and emulated mode, by setting this to true/false
// Currently, you need to comment out line 27 from /unittest/UnitTestController.php for this to work:
//            include_once( str_replace( UNITTEST_BASE.DIRECTORY_SEPARATOR, '..'.DIRECTORY_SEPARATOR, $file ) );
!defined('FTP_NATIVE') && define('FTP_NATIVE', false);
?>
====== getInstance ======
{#JAPI Joomla.Framework Session JSessionStorage::getInstance #}

Returns a reference to a session storage handler object, only creating it  if it doesn't already exist.

===== Syntax =====
database &getInstance	( **$name**, **$options** )

| **$name** | name | The session store to instantiate |
| **$options** |  |  |

===== Examples =====
<code php|Example>

</code>


----

~~DISCUSSION~~

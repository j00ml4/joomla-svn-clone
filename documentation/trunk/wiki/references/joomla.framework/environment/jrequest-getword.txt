====== getWord ======
{#JAPI Joomla.Framework Environment JRequest::getWord #}

Fetches and returns a given filtered variable. The word  filter only allows the characters [A-Za-z_]. This is currently  only a proxy function for getVar().<p>See getVar() for more in-depth documentation on the parameters.</p>

===== Syntax =====
string getWord	( **$name**, **$default**, **$hash** )

| **$name** | string | Variable name |
| **$default** | string | Default value if the variable does not exist |
| **$hash** | string | Where the var should come from (POST, GET, FILES, COOKIE, METHOD) |

===== Examples =====
<code php|Example>

</code>


----

~~DISCUSSION~~

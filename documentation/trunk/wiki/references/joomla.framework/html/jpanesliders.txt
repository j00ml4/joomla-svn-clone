====== JPaneSliders ======
{#JAPI Joomla.Framework HTML JPaneSliders #}

JPaneSliders is a class used to draw parameter panes. This child class implements the JPane interface and produces a set of pages that can be changed using buttons that appear at the top of the pane. Pages will slide in and slide out. JPaneSliders look like:

JPaneSliders are based on the mooFX library which Joomla! provides built-in support for. Since this library uses JavaScript, it is necessary to have JavaScript installed in the browser in order for this to work.

===== Methods =====
^ Method ^ Description ^
| {{references:Method.png?nolink}} [[JPaneSliders-__construct|__construct]] | Constructs the JPaneSliders class, adding the necessary CSS and JS to the current document. |
| {{references:Method.png?nolink}} [[JPaneSliders-endPane|endPane]] | Outputs the necessary HTML code to end a slider pane. |
| {{references:Method.png?nolink}} [[JPaneSliders-endPanel|endPanel]] | Outputs the necessary HTML code to end a slider panel. |
| {{references:Method.png?nolink}} [[JPaneSliders-startPane|startPane]] | Outputs the necessary HTML code and JavaScript to start a slider pane. |
| {{references:Method.png?nolink}} [[JPaneSliders-startPanel|startPanel]] | Outputs the necessary HTML code and JavaScript to create a slider panel with the title text and start the panel. |

----

~~DISCUSSION~~


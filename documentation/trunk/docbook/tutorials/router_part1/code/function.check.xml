<programlisting><![CDATA[function check()
{
       jimport( 'joomla.filter.output' );
       $alias = JOutputFilter::stringURLSafe( $this­>title );
       if(empty( $this­>alias ) || $this­>alias === $alias ) {
               $this­>alias = $alias;
       }
       /* All your other checks */
       return true;
}]]></programlisting>
